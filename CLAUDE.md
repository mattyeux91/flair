# Flair — simulation de monde persistant football

## Pitch

Un monde persistant de football qui vit avec ou sans joueur humain (façon Football Manager × Dwarf Fortress × Hattrick), simulé par un **noyau pur, déterministe**. Le client de jeu incarne un **agent de joueurs** (scouter, placer, négocier, gérer une carrière) — c'est la seule boucle de jeu humaine, et elle n'est pas encore validée.

Ce fichier est un **index et un aide-mémoire des règles chères à violer par accident**. Il ne remplace pas `docs/` : en cas de doute sur une formule, un exemple de code ou un détail, lire le document source plutôt que deviner.

## Index de la documentation (`docs/`)

| Doc | Contenu |
|---|---|
| `10-etat-de-l-art.md` | Références (FM, Hattrick, CK3, Dwarf Fortress, EVE), modèles académiques, ce qu'on emprunte à qui |
| `11-architecture-generale.md` | Noyau pur, hexagonale, intentions, CQRS, multi-monde, stack PHP, SOLID appliqué |
| `12-modele-du-monde.md` | ECS, entités/composants, singletons, vérité cachée vs perception, `Ruleset`, worldgen, invariants |
| `13-moteur-de-simulation.md` | Tick hybride, pipeline de systèmes, Scheduler, déterminisme, event sourcing, dimensionnement, boucle du Host |
| `14-algorithmes.md` | Moteurs de match (LOD), développement des joueurs, composition de facteurs, marché, économie, équilibre compétitif, narration |
| `15-roadmap.md` | Phases, critères de sortie, décisions à verrouiller avant de coder |
| `16-evenements-et-cascades.md` | Taxonomie Fait/DecisionRequest/Intent, seuils d'émission, contrôle des cascades, Event Monitor |
| `17-marche-transferts.md` | Chantier de suivi du lot 3 (Phase 2) : marché des transferts et inflation, découpé en 5 points vérifiables |
| `18-dettes.md` | **Le seul endroit où une dette attend.** Pas de déclencheur, pas d'entrée ; ce qui peut être mécanisé en sort pour devenir un test |
| `archive/ressource.md`, `archive/ressource2.md` | Sources internes non normatives (brouillon d'origine, déjà digéré par `10-`–`16-`) — ne pas les traiter comme référence de conception |

## Règles non négociables

- **Le noyau est une fonction pure, déterministe, zéro I/O** : `step(WorldState, TickContext): StepResult`. Aucun `rand()`/`mt_rand()`/`random_int()`, `time()`/`new DateTime()` sans argument, `getenv()`, accès disque ou réseau dans `kernel`. (`11-` §1, §9)
- **ECS strict, aucun sous-type.** `Player`, `Club`, `City` n'existent **pas** comme classes. Une entité est un entier ; « joueur » est le nom informel d'une entité qui porte des composants de compétences + `Contract` + `Fitness`. Les composants sont `readonly`, en lecture seule hors de leur système propriétaire. (`12-` §1, §2)
- **Vérité cachée vs perception.** `PlayerPhysicalSkills`/`PlayerTechnicalSkills`/`PlayerMentalSkills`/`PlayerPotentials` (la vérité) ne sont jamais exposés à un client. Ce qu'un observateur croit est **dérivé** à la lecture (bruit déterministe, fonction de `observerId`/`subjectId`/`observationCount`), jamais stocké. (`12-` §4)
- **Trois messages, jamais confondus** : Fait (passé, journalisé, `ctx->emit()`) / DecisionRequest (question transitoire, **jamais journalisée**, `ctx->ask()`, sort par `StepResult::$requests`) / Intent (futur immédiat, humain et PNJ via la même interface `IntentSource`). Les trois critères se lisent sur le **message**, pas sur son contenu : un message adressé à quelqu'un de précis, qui attend une réponse et qui expire, est une question — quoi qu'en dise son nom. (`11-` §3, `16-` §1)
- **Un événement n'est jamais traité dans le tick qui l'a produit.** Il part en OutQueue vers le tick suivant. C'est ce qui rend les cascades impossibles à boucler dans un même tick — structurel, pas une limite de profondeur. (`13-` §2, `16-` §3)
- **Un système n'appelle jamais un autre système**, directement ou indirectement. Il émet un événement ou écrit un composant lu plus loin dans le pipeline. Ça n'interdit **pas** les fonctions pures partagées (`Football\Support\`, sans état ni RNG ni accès au monde) : deux appelants d'une même formule ne se commandent pas l'un l'autre. Critère d'extraction : **deux consommateurs réels, jamais un seul, jamais par anticipation**. (`13-` §2, `11-` §9)
- **Seuils d'émission** : une mutation de composant n'est pas un événement. Un fait mérite d'être émis s'il franchit un seuil comportemental, est irréversible, ou est racontable — sinon le système se tait. (`16-` §2)
- **Itération toujours triée par `EntityId` croissant.** Jamais l'ordre d'une `Map`/d'un `Set`. Idem pour l'OutQueue et le Scheduler : ordre total imposé, jamais l'ordre d'insertion. (`12-` §2, `13-` §4.2/4.5)
- **PRNG 32 bits obligatoire.** PHP bascule silencieusement un dépassement d'`int` en `float` : un PRNG 64 bits naïf casse le déterminisme sans erreur. PCG32/xoshiro128\*\*, masquage `& 0xFFFFFFFF` après chaque opération. Un flux dérivé par `(worldSeed, tick, systemId, entityId)` — jamais un PRNG global partagé. (`11-` §6, `13-` §4.1/4.3)
- **Les singletons sont adressés par type, une instance unique** (`MarketInflation`, `SeasonPhase`, `WorldClock`). Une donnée qui varie par pays/région (`EconomicClimate`, `Weather`) est un **composant** d'entité, pas un singleton. (`12-` §3 bis — corrigé le 2026-07-31, cf. note dans le doc)
- **Règles paramétriques dans le `Ruleset` versionné** (JSON, schéma validé), jamais codées en dur. Un monde est épinglé à `(kernelVersion, rulesetVersion)` ; changer les règles d'un monde vivant est une migration explicite, pas un hot reload. (`12-` §6, `13-` §6)
- **Graphe de dépendances du monorepo**, à faire respecter mécaniquement (`deptrac`/`phparkitect`) dès que le code existe :
  ```
  kernel   → (rien)
  ruleset  → (rien)
  worldgen → kernel, ruleset
  host     → kernel, ruleset, worldgen
  api      → host
  harness  → kernel, ruleset, worldgen
  admin    → api
  game-web → api (HTTP uniquement)
  ```
  Si `kernel` importe quoi que ce soit d'autre, le build casse. (`11-` §7)

## Stack et conventions

- **PHP 8.3+** partout (kernel/host/api). Pas de Rust, pas de Node pour le noyau — le CPU n'est pas le facteur limitant (`13-` §7, `11-` §6).
- **PostgreSQL seul.** Pas de Redis/Kafka/Mongo. `jsonb` pour les événements, tables classiques pour les projections.
- **Aucun ORM dans `kernel`.** Doctrine autorisé uniquement côté projections/API.
- **Symfony ou Laravel** pour `host`/`api`/`admin` — **jamais** dans `kernel`, qui doit rester exécutable dans un test unitaire nu.
- **SSE**, pas WebSocket, pour le flux temps réel.
- Namespace racine `Flair\`, PSR-4, PSR-12. **PHPStan niveau max obligatoire sur `kernel`.**
- Monorepo Composer (path repositories) : `packages/{kernel,ruleset,worldgen,host,api,admin,game-web,harness}/`. Pas encore créé — voir « Où en est le projet ».

## Où en est le projet

**Stade actuel : Phases 0, 1, 2, 3 et 4 closes — la Phase 4 l'a été le 2026-08-09 (SSE requalifié en Phase 5, D7 et D8 soldées ; voir plus bas). Prochaine étape : la Phase 5, le jeu d'agent.** Phase 3 (Persistance et temps réel) — **son critère de sortie est atteint** le 2026-08-08 (`15-roadmap.md` §4). Trois lots : sérialisation du `WorldState`, extraction de `packages/worldgen`, puis `packages/host` (un monde vit en base).** La seconde moitié du critère de sortie de la Phase 2 a dû être *réécrite* avant d'être cochée : voir la note ⚠️ dans `15-` §4 Phase 2. Phase 0 vérifiée empiriquement le 2026-08-02 via `packages/harness/bin/aggregate.php` (40 saisons, seeds 42 et 7, 500 joueurs/18 clubs) : effectif stationnaire ~313-329 dès l'année ~13, répartition domicile/nul/extérieur 41.8/29.6/28.6% (proche du réel), 11 champions différents sur 18 clubs en 19 saisons (deux clubs à 4 titres chacun, aucun quasi-monopole). La fenêtre "20 saisons" du critère initial était trop courte pour une population de départ pas encore à l'équilibre d'âge — voir la note empirique dans `15-roadmap.md` §4. Phase 1 vérifiée le même jour : comparaison à graines appariées complète (baseline + `--set` + delta Gini/rotation) en ~1min49s, sous la barre des 5 minutes du critère de sortie ; Gini des titres et métriques de graphe d'événements, test de déterminisme état+séquence d'événements, et CI (`kernel` puis `harness`) tous en place. En parallèle, le prototype papier/CLI de la boucle de jeu de l'agent (`prototype/agent-loop/`) a confirmé que la tension commission/satisfaction/réputation est un vrai dilemme — conservé tel quel, en l'état de prototype jetable, pas promu en doc ni supprimé.

**Phase 2, avancement (`15-` §4, critère de sortie double : conservation monétaire + inflation dans la cible)** : le grand livre monétaire est posé — `Finances`/`Contract`/`SeasonIncome`, singleton `MonetaryMass`, `Football\FinanceSystem` (revenus de fin de saison répartis entre part égale et part au mérite via `meritShare`, salaires hebdomadaires, entretien et investissement en installations). `Football\FacilitiesSystem` referme la boucle « résultats → revenus → installations → entraînement/intake → compétences → résultats » (`docs/14-` §7). L'entretien des installations est **convexe** (coût ∝ carré de la qualité, pas linéaire) depuis le 2026-08-02 : c'est l'amortisseur mécanique de cette boucle, retenu à la place d'un mécanisme endogène type conseil d'administration/limogeage (hors périmètre Phase 2, pas de gouvernance de club) — mesuré sur graines appariées, Gini des titres 0.497→0.363 (défaut) et 0.717→0.628 (`meritShare=0.6`). Moitié « conservation monétaire » du critère de sortie mécanisée par `Harness\Tests\Regression\MonetaryConservationTest` (verte sur 20 saisons, cas plat et cas au mérite). `Contract` porte désormais `expiresOn`, et le couple `Football\ContractSystem`/`Football\SquadSystem` fait vivre le mercato annuel : expiration, renouvellement au prix du marché (`Football\Support\WageModel`, premier vrai prix du monde — `base × clamp(qualité/référence, 0.4, 2.5)`), et joueurs sans club repris par les clubs en déficit. Aucune indemnité de transfert (hors périmètre), donc aucun argent ne change de mains et la conservation monétaire reste vraie sans y toucher. Mesuré le 2026-08-02 sur six graines appariées : **rotation du top 5 de 47,8 % à 53,3 %, sur 6 graines sur 6** ; **Gini des titres sans effet détectable** (0,528 → 0,557, très en dessous de la dispersion entre graines 0,363-0,614 — un Gini lu sur une seule graine est du bruit). Un plancher d'effectif essayé pour corriger le fait que `MatchSystem` note la *moyenne* d'un effectif quand le budget en contraint le *total* s'est mesuré nuisible (rotation retombée à 49,2 %) et a été retiré ; la vraie correction appartient au moteur de match.

**Ordre des lots restants, révisé le 2026-08-04 (`15-` §4 Phase 2 pour le raisonnement complet)** — deux lots faits, **un seul restant** (le 3) :
1. ~~Les onze meilleurs dans `MatchSystem`~~ — **absorbé par le lot des postes, fait le 2026-08-04.** Il ne tenait pas debout séparément : la sélection d'un onze fondée sur un agrégat aveugle aux postes aurait été réécrite intégralement par le lot suivant. **Lot des postes livré** : `Position` (GK/DEF/MID/ATT), `Football\Support\PositionModel` (matrice de contribution façon Hattrick, source de vérité unique des poids), `Ruleset\PositionBalance`, un potentiel qui plafonne une **composition** et non chaque compétence, le onze composé par poste dans `MatchSystem`, `WageModel::quality()` devenue « note au meilleur poste », conscience minimale des postes dans `ContractSystem` et `YouthIntakeSystem`. Mesures : écart-type des attributs dans un même joueur 4,0 → 16,7 ; dans le profil d'un poste 1,5 → 8,6 ; club-années sans gardien 7,87 % → 1,39 %. Campagne à 6 graines appariées : **aucun effet sur l'équilibre** (Gini des titres et rotation du top 5 tous deux à 3 hausses / 3 baisses — c'est le résultat enregistré *avant* l'implémentation), victoires à domicile 42,6 % → 42,0 %, population stable, et **aucun recalibrage de `strengthScale`**. Limites assumées : le poste dérivé coïncide avec l'archétype dans 100 % des cas, une seule formation, aucune tactique, et 1,39 % de club-années sans gardien que seul le marché des transferts fermera.
2. ~~**Perception/scouting**~~ — **fait le 2026-08-05.** `ContractSystem` ne lit plus la vérité cachée : toutes ses décisions passent par une qualité **perçue** par le scout que le club emploie, et un club sans recruteur est le **pire** observateur du monde, jamais un omniscient. Livré : `Employment` + `Scout` (premier rôle non-joueur du monde, `observerId` est bien une **personne**), `Ruleset\PerceptionBalance`, `Football\Support\PerceptionModel` (pure, Irwin-Hall plutôt que Box-Muller — aucune transcendante de plus), `Contract::$signedOn` (porte `observationCount` sans structure par paire), `Core\SystemContext::stableHash()` (dérivation **invariante par tick**, sans `systemId`, pour que le futur marché perçoive un joueur comme le système de contrats). Aucun mécanisme d'observation, aucun rôle autre que scout, aucune embauche — Phase 5 / gouvernance de club. Trois écarts au plan, tous documentés : la formule de σ diffère de l'esquisse de `docs/12-` §4 (dans l'esquisse, un jugement de scout n'aide *pas* sur un joueur jamais observé — l'inverse du métier) ; le **poste** reste lu sur la vérité ; et la corrélation jugement ↔ classement n'est pas calculée, seulement affichée à côté du classement. Mesuré sur 6 graines appariées (perception contre `--set baseErrorPoints=0`) : **aucun effet sur l'équilibre compétitif** — Gini des titres et rotation du top 5 tous deux à 3 hausses / 3 baisses. La prédiction écrite avant l'implémentation (« hausse du Gini, handicap permanent ») était **fausse** : elle semblait confirmée sur la seule graine 42 (0,446 → 0,509), c'est le même piège du Gini mono-graine qu'au lot des contrats. Seul effet consistant : **masse salariale +0,54 %, 5 graines sur 6** — la malédiction du vainqueur, gratuitement (un club signe les joueurs qu'il surestime, donc la population sous contrat est biaisée vers les surévalués). Faible en amplitude, mais c'est le mécanisme que le lot marché va amplifier. Expérience de contrôle `--scout-judgement-spread=0` (tous les recruteurs égaux, comparaison appariée elle aussi) : **la hiérarchie de staff ne produit pas davantage d'effet mesurable** — Gini 4/6, champions distincts 3/6, indistinguables du hasard. Autrement dit `baseErrorPoints = 10` place le monde dans un régime où **la perception change les prix, pas les classements** ; pour qu'elle compte davantage il faudrait monter l'amplitude, ou rendre le jugement **endogène** (club riche → meilleur recruteur), ce qui est la gouvernance de club et donc hors Phase 2. Coût CPU +2 % (72,5 s → 74,0 s sur 40 saisons). **Fait de méthode à retenir : aucun lot qui ajoute une entité au genesis ne peut être une réduction bit-à-bit d'un monde antérieur** — les 18 scouts consomment 18 `EntityId`, ce qui décale l'allocateur pour toutes les entités créées ensuite à l'exécution, donc leurs flux RNG ; vérifié en faisant consommer les mêmes identifiants à un arbre pré-lot, qui redevient alors identique au chiffre près. Une comparaison à graines appariées se fait toujours **dans un même build**, jamais contre des nombres notés dans un document — y compris ceux-ci.
3. **Marché des transferts et inflation, un seul lot** (`docs/14-` §5) : indemnités, valorisation sur qualité **perçue**, négociation multi-tours, agents PNJ, `marketInflationTarget` et son régulateur. La moitié « inflation dans la cible » du critère de sortie **ne peut pas** être un lot séparé : le seul prix du monde étant `base × clamp(...)` avec `base` constante du `Ruleset`, l'inflation vaut identiquement zéro par construction et un régulateur ne régulerait rien tant qu'aucun argent ne poursuit un actif rare. Les indemnités rendent aussi `MonetaryConservationTest` non trivial pour la première fois.

   **En cours, découpé en 5 points suivis dans `docs/17-marche-transferts.md` — 4/5 au 2026-08-07, un commit par point.** Faits : (1) `Football\Support\MarketValueModel`, la valorisation pure ; (2) `Football\TransferSystem` + composant `Negotiation`, la négociation multi-tours en PNJ seul — **premier état multi-tick du noyau** (même composant dans `creates()` et `writes()`, cf. `SystemAccess::requiresCreatedEntity()`), mesuré à médiane 2 tours donc le risque « converge instantanément » de `docs/14-` §5 est écarté, avec la réserve honnête que 44,9 % se résolvent quand même au tour 1 ; point 2 réouvert pour `BoardPatience`, la patience individuelle du club vendeur ; (3) `Football\Intents\` — les décisions de l'acheteur passent par `BuyerIntentSource`, dont `NpcBuyerIntentSource` et `SubmittedBuyerIntentSource` sont deux implémentations. Écart tranché : interface **de domaine**, pas le `Core\IntentSource` + `WorldView` générique de l'esquisse de `docs/11-` §3, qui aurait été une abstraction posée par anticipation avec un seul consommateur. Le vrai contenu du point 3 n'était pas l'interface mais le **découpage en ticks** : la contre-demande est posée au tick N et la réponse lue au N+1, sans quoi un humain répondrait à une question qu'il n'a pas encore vue — re-mesure identique au chiffre près (715 négociations, médiane 2, moyenne 2,81, 44,9 %), le refactor est neutre en comportement ; (4) **les indemnités se paient**. Le design n'avait aucune latitude : « un seul writer par composant » désigne `FinanceSystem` pour `Finances`/`MonetaryMass` et `SquadSystem` pour `Contract`/`SquadMembership`, donc `TransferSystem` décide en queue de pipeline et les deux appliquent au tick suivant — `TransferAgreed` devient exécutoire, `ContractSigned` (émis en même temps) déplace le joueur. Une indemnité **n'est ni une injection ni un puits** : `MonetaryMass` ne bouge pas, ce qui rend `MonetaryConservationTest` non trivial pour la première fois (garde-fou ajouté : le test exige qu'il ait réellement circulé des indemnités). Pas de Fait `TransferCompleted`, il n'aurait rien ajouté aux deux autres. Solvabilité ajoutée comme **politique de PNJ** (le plafond est borné par le solde), pas comme règle du système. Mesuré sur deux graines : négociations 715 → 590, part au premier tour 44,9 % → 39,7 % (la réserve du point 2 s'améliore toute seule), et surtout **le marché ne concentre rien** — aucun solde négatif, Gini des soldes de club 0,011/0,008, indemnités totales marginales devant la masse en circulation. Le marché est réel mais **économiquement inerte**, parce que le PNJ maximise `qualité/prix` donc chasse les fins de contrat que `MarketValueModel` brade (indemnité médiane à 9 % de `baseValueCents`). C'est le cadrage du point 5 ; (5) **`indice_inflation_global` et sa régulation**, le point qui a le plus dévié de sa conception. Trois mesures ont interdit la forme prévue : ~3 transferts par saison rendent **aucun indice de prix calculable** ; la masse monétaire est négative neuf saisons puis **traverse zéro**, donc inutilisable en dénominateur ; et surtout **le monde n'a aucune inflation endogène** (sans régulateur, masse et masse salariale plates trente saisons — salaires et valeurs sont des formules du `Ruleset`, pas des prix d'équilibre). L'indice est donc posé comme une **décision de politique monétaire** qui multiplie *tout* ce qui est nominal, et le taux réalisé égale la cible **par construction**. L'asservissement des injections a été **construit, mesuré instable deux fois, retiré** (dénominateur endogène qui bouge dans le mauvais sens) au profit d'une boucle ouverte : indice + terme d'anticipation `cible × masse`. `Football\Singletons\MarketInflation` (second singleton), `Ruleset\InflationBalance`, `ClubInvestedInFacilities` gagne `referenceCents` parce que `FacilitiesSystem` **ne peut pas** lire l'indice (cycle dans `SystemGraph`). **Défaut à `0.0`, no-op strict vérifié au centime** ; à 3 % le monde reste stable mais le chômage tombe de ~35 à ~2 — mesuré, non corrigé, d'où le défaut à zéro. Piège PHP à retenir : `(int) round(PHP_INT_MAX * 1.0)` rend un entier **négatif**, et le `min`/`max` naïf n'y échappe pas (`(float) PHP_INT_MAX` vaut 2⁶³) — `FinanceSystem::scaled()` clampe par comparaisons explicites.

**Phase 3, avancement (`15-` §4)** — **lot 1 fait le 2026-08-08 : le `WorldState` se sérialise.** `Core\Snapshot\` dans le kernel (pur, zéro I/O : le format appartient au noyau, le Host ne stockera que des octets) — `TypeRegistry` (clé stable ↔ classe, **aucun FQCN dans le payload**, et le même registre alimentera `events.type`), `SnapshotCodec`, `ValueCodec` réflexif, `SnapshotContract`, l'attribut `SnapshotArrayOf`, l'enveloppe `WorldSnapshot`, `JsonSnapshotFormat`, plus `Kernel::VERSION` et `Football\FootballTypes`. Trois corrections apportées à la conception écrite : **le tick n'est pas dans le `WorldState`** (`13-` §8 écrivait `$state->tick + 1`, qui n'existe pas — il vit dans `TickContext`, d'où l'enveloppe) ; **le rejeu du delta est abandonné** au profit d'un snapshot par tick dans la transaction des événements (mesuré 500 joueurs/18 clubs/12 saisons : 0,38 Mo par snapshot, 6,9 ms pour encoder, 13,7 ms pour relire, contre 6,1 ms de coût moyen d'un tick — à 1 tick/h rejouer n'achète rien et rouvre le piège du `13-` §6) ; et **la conformité est mécanisée** (`SnapshotConformanceTest` balaie le disque et exige que tout type du domaine soit enregistré **ou atteignable** depuis un type enregistré — une liste à la main aurait eu le défaut qu'on corrigeait). Le critère de sortie de la phase est déjà sous test **sans base de données** : `Harness\Tests\Regression\SnapshotContinuityTest` interrompt un run au premier tick où l'OutQueue, le Scheduler puis une `Negotiation` sont réellement occupés, ne garde que la chaîne JSON, et exige une suite indiscernable — même hash d'état **et** même hash d'événements, avec échec si l'une des trois structures n'a jamais été couverte. Éprouvé par trois sabotages du codec, tous détectés. Trou fermé au passage : `WorldHasher` tenait sa propre liste de types, sans `BoardPatience`, `Negotiation` ni `MarketInflation` — **tout le marché des transferts était hors du test de déterminisme depuis le lot 3** ; elle dérive maintenant de `FootballTypes`. Non-régression vérifiée dans un même build (autoloader pointant tour à tour sur l'arbre pré-lot et l'arbre courant) : état et séquence d'événements **identiques au chiffre près**. **Lot 2 fait le 2026-08-08 : `packages/worldgen`.** Un déblocage, pas une fonctionnalité — `host` doit pouvoir créer un monde et le graphe de `11-` §7 lui interdit d'importer `harness`. `WorldFactory` (ex-`PopulationFactory`), `ClubFactory`, `CompetitionFactory` et `StaffFactory` ont quitté `harness/src/Population/` ; `Worldgen\WorldSpec` porte la forme du monde, `Harness\Population\PopulationSpec` survit avec le seul `years` (une durée de run n'est pas une propriété du monde) et **garde sa signature à plat**, en gagnant un `world()` — les ~20 sites de construction n'ont pas bougé. Le seul critère de réussite d'un lot pareil est l'**exactitude bit-à-bit**, que les suites de tests ne peuvent pas prouver (elles comparent des runs entre eux dans un même build) : vérifié par empreinte, arbre pré-lot contre arbre courant — état, séquence d'événements, compteur d'entités et `EntityId` joueurs **identiques**. ⚠️ Règle d'or à ne jamais oublier dans `worldgen` : **l'ordre des tirages RNG et des allocations d'`EntityId` est une donnée d'architecture**, le réordonner change le monde entier (cf. les 18 scouts du lot perception) — trois précautions du code portent cette contrainte, détaillées dans `packages/worldgen/README.md`.

**Lot 3 fait le 2026-08-08 : `packages/host`, et le critère de sortie de la Phase 3 est atteint.** Trois tables (`worlds`, `events`, `snapshots`), `AdvanceWorld`/`CreateWorld`, verrou `pg_try_advisory_xact_lock`, CLI `bin/host.php`, PostgreSQL en conteneur (`docker-compose.yml`, port **54329** pour ne pas heurter le postgres local de la machine). **Stack : `illuminate/database` seul, pas de skeleton Laravel** — `api → host` fait de `host` une *dépendance*, et deux skeletons d'application ne se composent pas ; Laravel complet ira dans `api` en Phase 4. Le CLI est un script nu (idiome `harness/bin/*.php`) ; `symfony/console` s'ajoutera s'il dépasse ~8 commandes, c'est la couche sur laquelle Artisan lui-même est bâti.

**Tout tient dans une seule transaction** — verrou, lecture du snapshot, `step()`, Faits, snapshot, tick. C'est cette atomicité seule qui rend le critère vrai. `Host\Tests\CrashRecoveryTest` le vérifie au sens littéral (sous-processus réel, **SIGKILL** en plein vol, trois fois), avec deux assertions : base cohérente, **et** monde repris identique à un monde jamais interrompu. Sabotage : sans transaction, détecté 4/4 ; avec deux transactions successives, 2/6 — filet probabiliste assumé, la garantie est structurelle.

⚠️ **Trois pièges mesurés, à ne pas réapprendre.** (1) **Le coût d'écriture en base ≈ le coût du noyau** : 17,8 ms/tick contre 18,7 ms de simulation (500 joueurs/18 clubs) — première confirmation chiffrée de `13-` §7. (2) **`jsonb` réordonne les clés d'objet** : un snapshot relu depuis `jsonb` n'est plus identique octet pour octet à ce que le noyau a produit ; `snapshots.state` est donc en **`json`**, `events.payload` reste en `jsonb` (les projections devront l'interroger). (3) **Le verrou advisory survit 0,7 à 3,4 ms au processus tué** — sans conséquence pour un cron horaire, fatal pour un test qui enchaîne kill et reprise immédiate.

Restent : projections (Phase 4, jamais nécessaires — zéro table de projection, décision tenue) et SSE, **requalifié en Phase 5 le 2026-08-09**. Le jour venu, **les projections rejoindront cette même transaction** et **la diffusion SSE restera dehors** — publier avant le commit annoncerait un tick qui peut encore être annulé.

**Phase 4 (API + admin) ouverte le 2026-08-08 — lot 0 fait : un monde de dix ans existe en base, et on sait ce qu'il coûte.** Découpage retenu (5 lots, `docs/15-` §4 Phase 4) : 0. le jeu de données ; 1. `packages/api` + une couche de lecture + la fiche d'un club ; 2. l'histoire d'un club sur dix ans ; 3. le digest cadré **club** ; 4. SSE en dernier (hors critère de sortie, et à un tick/heure il ne vaut pas mieux qu'un rafraîchissement). Deux décisions prises d'entrée : **un seul paquet `api` qui sert le JSON *et* les pages Blade de l'admin, pas de `packages/admin`** — écart assumé au graphe de `11-` §7, l'admin est un outil interne mono-utilisateur et un SPA n'y achète rien, la non-divergence étant tenue par une **couche de lecture unique** (`Api\Read\`) et non par la séparation en paquets ; et **zéro table de projection** jusqu'à ce qu'un écran soit mesuré trop lent, l'event log et le snapshot répondant aux deux moitiés du critère de sortie, `snapshot + PerceptionModel` à la lecture étant d'ailleurs la seule forme conforme à `12-` §4.

Chiffres du lot 0 (détail dans `packages/host/README.md`, re-mesurés après le lot des dettes) : 3 650 ticks en **181 s** (49,7 ms/tick), **4 610 Faits** (~460/saison), `events` **2,1 Mo**, `snapshots` 100 Mo → **808 ko** après `VACUUM FULL`. Quatre choses à ne pas réapprendre. (1) ~~**`advance` sous-estime son propre coût**~~ — **corrigé le 2026-08-08**, et l'écart avait un visage : `49,7 = simulation 24,9 + persistance 17,9 + verrou/commit 6,8`. **Charger le snapshot coûte ~6 ms** (un quart du « temps de simulation ») et le commit presque autant, donc le noyau proprement dit ne pèse que ~19 ms sur 49,7 — **les trois cinquièmes d'un tick sont de la base**, pas seulement son écriture comme l'annonçait `13-` §7. (2) **L'histoire d'un club n'a besoin d'aucun index** : `Seq Scan` à **2,17 ms** sur dix ans, la table entière tenant en cache — et le club n'a même pas de clé unique dans les payloads (`homeClubId`/`awayClubId` pour `MatchPlayed`, `clubId` pour `ContractSigned`, la **position** dans `finalTable` pour `SeasonConcluded`), donc son histoire est une **union de prédicats par type**. (3) **Le ballonnement de `snapshots` n'est pas une fuite** : autovacuum a bien ramassé, mais PostgreSQL garde les pages réutilisables au lieu de les rendre à l'OS — 100 Mo est un plafond, pas une croissance. (4) **60 % des Faits sont des `MatchPlayed`** : trois mois de digest, c'est ~115 Faits dont ~70 matchs — un journal, pas une histoire. C'est le contrôle qualité des seuils que `14-` §9 promettait, et c'est le cadrage du lot 3, pas une raison d'avancer la narration de la Phase 6.

⚠️ **Trou fermé au passage, et c'en était un vrai** : `Host\Tests\PersistedWorldMatchesMemoryTest` tournait sur **120 ticks**, alors que `CalendarBalance::$seasonStartDayOfYear = 0` et un monde né au tick 0 ne génèrent leur première saison qu'au **tick 365**. L'aller-retour en base n'avait donc **jamais traversé un match, une fin de saison, un contrat renouvelé ni une négociation** — toute la Phase 2 était hors du seul test qui garantit que persister ne change rien au monde. Porté à **575 ticks / 4 clubs / 60 joueurs**, ce qui fait traverser la base à une `Negotiation` **en cours de vol**. La couverture est **vérifiée et pas supposée** (dix types de Faits exigés, sabotage à 120 ticks détecté : le run n'y rencontre que `player_retired`). Deux faits de calibrage à ne pas perdre : **le marché n'ouvre qu'au jour 200 de l'année 2**, jamais de l'année 1 (au genesis aucun club n'est en manque) ; et **au-delà de ~20 joueurs par club le marché ne s'ouvre plus du tout** (mesuré : 4/100, 4/140, 6/150, 8/200 → zéro négociation en 600 ticks) — un effectif large est un monde *sans* marché, contre l'intuition.

**Lot 1 fait le 2026-08-08 : `packages/api`, et le monde est visible.** Laravel 13 complet, `Flair\Api\Read\` (couche de lecture unique : `WorldReader`, `LoadedWorld`, `ClubSheetReader`, `WorldSummaryReader`, `StandingsReader`, `WorldListReader`, les DTO `Read\View\*`, `Format\Money`), trois routes HTML plus les mêmes DTO en JSON, et le garde `Ruleset` côté `host`. **Détail dans `packages/api/README.md`, pas dupliqué ici.** Coût réel mesuré sur `dix-ans` : index **17,2 ms** (aucun snapshot décodé), fiche de club **28,7 ms**, JSON de la même fiche 28,1 ms — les 14 ms de décodage sont la moitié du coût, HTML et JSON coûtent la même chose. 7 tests, 240 assertions ; PHPStan niveau max **sans erreur, couche HTTP incluse**.

⚠️ **`packages/api` a deux racines d'autoload, et c'est mécanisé, pas conventionnel** : `src/` → `Flair\Api\` (la lecture du monde, **PHP nu**) et `app/` → `App\` (l'adaptation à Laravel), la dépendance étant à sens unique. Deux raisons — la convention `Flair\` du dépôt, et un cœur de lecture substantiel que le digest, SSE et `game-web` réutiliseront ; la troisième que j'avais avancée (« le jour où l'admin sortira en paquet séparé ») est un bonus, **pas** une justification, c'était de l'anticipation. Parce que deux racines coûtent une décision par fichier, et qu'une frontière qui coûte ça sans rien garantir est **pire que pas de frontière**, elle est tenue par deux choses éprouvées par sabotage : `Api\Tests\Architecture\ReadLayerStaysFrameworkFreeTest` balaie le disque (même idiome que `SnapshotConformanceTest`) et interdit à `src/` d'importer `Illuminate\`/`Symfony\`/`Laravel\`/`App\` et à un contrôleur d'importer `Host\Store\*` ; et `Api\Tests\ReadTestCase` **n'étend pas** la classe de test de Laravel, ce qui *prouve* que la couche de lecture n'a besoin d'aucun framework. Si un jour cette frontière gêne à l'usage, la bonne réponse est de tout ramener sous `App\` et de l'assumer, jamais de la laisser mentir.

Cinq choses à ne pas réapprendre. (1) **Le test qui tient l'écart au graphe de `11-` §7** : un seul paquet sert l'admin *et* l'API, et ce n'est pas la séparation en paquets qui empêche les deux présentations de diverger, c'est qu'elles n'ont qu'une source — `Tests\Http\PagesMatchJsonTest` prend les chiffres du JSON, les met sous la forme de la page, et exige de les y retrouver, club par club et joueur par joueur. Sans lui l'écart serait une intention. (2) **La perception n'est pas dans ce lot, seul son site est nommé** : `ClubSheetReader::qualityOf()`, et nulle part ailleurs. Pas d'interface à une implémentation, pas de paramètre de point de vue sans second appelant, pas de `truthVisible` sans client de jeu — le DTO ne changera pas de forme parce que `PerceptionModel::estimate()` rend un `int` sur la **même échelle 1-100** que `WageModel::quality()` (vérifié). (3) **Le drapeau « vérité visible » appartiendra au monde, pas au client** : deux droits distincts qu'un seul drapeau ne peut pas porter — ce que voient les *joueurs* est une option du monde (dans un monde partagé, un client qui décide seul obtient un avantage sur les autres agents du même marché, et le scouting ne vaut plus rien), ce que voit l'**exploitant** n'est pas une question de jeu et vaut toujours tout. (4) **Les `path` repositories de Composer ne sont pas transitifs** : `api` doit déclarer `../worldgen` et `../kernel` en plus de `../host`, sinon la résolution échoue sur `flair/kernel @dev could not be found`. (5) **`WorldState::components()` est déjà génériquement typé** (`@template T of object`) : `->components(Club::class)->get($id)` est vu `?Club` par PHPStan, donc lire l'ECS hors d'un système ne demande **aucun** équivalent de `Host\Store\Row` — la prédiction inverse écrite dans le plan était fausse.

⚠️ **Garde `Ruleset` posé dans `host` au passage, et ce n'était pas le bug annoncé.** `rulesetVersion` n'est pas une version mais une **étiquette libre** dont le harness se sert comme telle (`'harness'`, `'ci'`, `'snapshot-continuity'`…), donc le noyau doit continuer à laisser passer n'importe quelle chaîne : le garde appartient aux mondes **persistés**, où la chaîne sert à *reconstruire* les règles des mois plus tard. `Host\Rules\RulesetForWorld` + `UnsupportedRulesetVersion` : `AdvanceWorld` faisait `new Ruleset($world->rulesetVersion)`, ce qui rendait les défauts du noyau quelle que soit la version épinglée. Pire trouvé en explorant : **le paramètre `Ruleset` de `CreateWorld` était un piège complet** — `WorldFactory::populate()` accepte des groupes de `Balance` que `CreateWorld` ne lui passait pas, donc un `Balance` sur mesure était ignoré au genesis *puis* reconstruit aux défauts à l'avancement ; il prend maintenant une **chaîne**. Effet plus intéressant que le garde lui-même : tant qu'une seule version est acceptée, genesis et avancement lisent **forcément** les mêmes règles — la classe de désaccord devient inatteignable, pas seulement bruyante. C'est le seul site que `packages/ruleset` aura à rebrancher.

**Lot 2 fait le 2026-08-08 : dix ans d'histoire d'un club, en blocs par saison.** `Host\Store\EventStore::between()` + `RecordedEvent` (le **miroir exact** de `append()` : `keyFor()` à l'écriture, `classFor()` + `ValueCodec` à la lecture, donc des Faits **réhydratés en objets typés**) ; `Api\Read\History\{ClubMentions,ClubMention,ClubRole,ClubHistoryReader}` et les DTO. **Le noyau n'a rien eu à changer.** 17 tests / 888 assertions sur `api`, 17 tests / 202 assertions sur `host`, PHPStan max clean partout. Page à **58,7 ms**, JSON 55,4 ms (club 11 de `dix-ans` : 428 Faits retenus sur 4 610, dont 14 retraites sur dix ans).

Quatre choses à ne pas réapprendre. (1) **Un club n'a pas de clé unique dans les payloads** : il apparaît sous six noms (`clubId`, `previousClubId`, `homeClubId`/`awayClubId`, `buyerClubId`/`sellerClubId`) et `SeasonConcluded` l'encode par la **position** dans sa table finale. L'histoire d'un club est une union de cas, déclarée une seule fois dans `ClubMentions`, et `EveryFactIsPlacedOrExcludedTest` exige que tout Fait enregistré soit traité **ou** exclu avec sa raison. (2) **Une prolongation n'est ni une arrivée ni un départ** : 753 des 819 `ContractSigned` du monde de référence ont le même club avant et après (25 vrais transferts, 41 signatures de joueurs sans club). La première version comptait tout en arrivées et la page annonçait « 7 arrivées » pour un club qui n'avait recruté personne — **vu en ouvrant la vraie page, pas par un test**. (3) **Le seau de saison 0 n'est pas la première saison de compétition** : `intdiv(tick, 365)` et une saison générée quand `tick % 365 === 0` la placent au tick 365, donc dans le seau **1** ; le seau 0 est l'année du genesis, avec du mercato et aucun match. (4) ~~**Le rang est un Fait, les points sont un calcul**~~ — le recalcul depuis les `MatchPlayed` et `CompetitionBalance` **n'a survécu qu'à la saison en cours**, voir le lot des dettes. `Host\Rules\RulesetForWorld` y trouve quand même son second consommateur.

⚠️ ~~**Deux Faits ne peuvent pas entrer dans l'histoire d'un club**~~ — **soldé le 2026-08-08, voir le lot des dettes ci-dessous.**

**Lot des dettes, fait le 2026-08-08 : les Faits disent enfin qui ils concernent.** Le lot 2 avait mis à nu une famille de défauts qu'aucun test ne pouvait voir tant que personne ne relisait le passé, et elle **avait une date de péremption** : `events.payload` n'a aucune colonne de version de format et `Core\Snapshot\ValueCodec` est strict dans les deux sens (champ absent ⇒ lève, champ inconnu ⇒ lève, `isDefaultValueAvailable()` jamais consulté), donc **ajouter un champ à un Fait rend illisible tout Fait déjà en base**. Gratuit tant qu'aucun monde ne compte, migration ensuite. Trois Faits corrigés : `PlayerRetired` porte `?int $clubId`, `TransferCounterDemanded` porte ses deux clubs, et `SeasonConcluded.finalRanking: list<int>` devient **`finalTable: list<StandingsEntry>`** plus un accesseur dérivé `ranking()`. La règle générale qui en sort est écrite dans `docs/16-` §2 : **un Fait porte de quoi l'attribuer à ses sujets**, le critère pratique étant « si l'état du monde était effacé, cette ligne se comprendrait-elle encore ? ». `ClubMentions::NOT_ABOUT_A_CLUB` ne compte plus que **trois** exclusions, toutes de vraies décisions.

Six choses à ne pas réapprendre. (1) **Renverser la décision de `SeasonConcluded` était juste, et le docblock d'origine avait tort** : il refusait les points au motif d'« une copie de `Standings`, deux sources de vérité » — mais `Standings` est écrasé au `SeasonStarted` suivant, donc pour une saison écoulée il n'y avait pas deux sources, il n'y en avait **aucune**. Le recalcul côté lecture n'était juste **que par accident** (il suppose que les points ne viennent que de résultats de match). Une saison conclue est désormais **citée**, une saison en cours **comptée**, et deux tests exigent que les deux chemins coïncident. (2) **Ajouter `Contract` à `reads()` de `RetirementSystem` n'a pas bougé l'ordre dérivé du pipeline, et c'est démontrable** : `SquadSystem` est le seul writer de `Contract` et passait déjà avant, donc la seule arête ajoutée était déjà satisfaite, et le Kahn de `SystemGraph` (« premier prêt dans l'ordre déclaré ») produit la même sortie. Vérifié : ordre identique. (3) **Une garde qui compare une chose à elle-même ne garde rien** : `SquadSystem` relisait `Contract` pour passer son `clubId` à `release()`, qui le comparait ensuite au même contrat. Remplacé par `expectedClubId: null` — comportement identique, intention lisible, et c'est le bon comportement (une retraite délie de qui que ce soit, contrairement à une expiration). (4) **Le monde régénéré est le même au Fait près** : mêmes 4 610 Faits, **même compte pour chacun des douze types**. Seuls les payloads grossissent (`events` 1,5 → 2,1 Mo). C'est la seule preuve qui vaille qu'un changement de payload n'a rien changé au monde. (5) **`harness/public/index.php` était cassé depuis le lot worldgen** et décrivait 43 champs sur 82, laissant **cinq groupes rendre des `<details>` vides** — invisible parce que le fichier était hors PHPStan *et* hors tests. Réparer sans mécaniser aurait remis le compteur à zéro : `Harness\Web\CalibrationFields` sort la liste du script pour qu'un test la confronte à `RulesetOverride::ALL_FIELDS` dans les deux sens, `Harness\Web\Input` rend le niveau max tenable sur du `mixed` de superglobale, et `public/` est entré dans `phpstan.neon`. ⚠️ **La CI doit appeler `composer analyse`** et non `vendor/bin/phpstan analyse` : 128 Mo ne suffisent plus. (6) **`Person` n'est plus une dette mais une décision** : le lot 2 lui a trouvé un usage réel (le nom d'un joueur parti reste lisible dans l'histoire d'un club), la rétention est écrite dans le docblock de `RetirementSystem` avec son prix — l'état d'un monde croît linéairement avec son histoire, à revoir seulement si un monde vit un siècle.

⚠️ **`AdvanceWorld` ne sous-estime plus son coût, et l'écart avait un visage.** `AdvanceResult::$totalSeconds` entoure la transaction entière (le `COMMIT` tombe après le retour de la closure, hors de portée de tout compteur interne) et le chronomètre de simulation englobe désormais `SnapshotStore::latest()`. Mesuré sur `dix-ans` : **49,7 ms/tick = simulation 24,9 + persistance 17,9 + verrou/commit 6,8**. Donc **charger le snapshot coûte ~6 ms**, un quart de ce qu'on appelait « simulation », et le noyau proprement dit ne pèse que ~19 ms sur 49,7 — **les trois cinquièmes d'un tick sont de la base**, pas seulement son écriture comme l'annonçait `13-` §7. `bin/host.php destroy <monde> --force` complète le CLI (régénérer un monde après un changement de payload n'est plus une incantation SQL) et `DatabaseTestCase::forget()` passe par lui, ce qui fait du nettoyage de chaque test un exercice du chemin de production.

⚠️ **Fait de méthode, à ne pas perdre** : ce lot n'a **pas** corrigé la boucle `advance --ticks=N`, que j'avais annoncée buguée en planifiant. Elle ne l'est pas — un `if` juste après la boucle rattrape le dernier tick. Lire jusqu'au bout avant d'inscrire un bug dans un plan.

**Lot 3 fait le 2026-08-08 : le digest de retour d'absence, et le critère de sortie de la Phase 4 est atteint.** `Api\Read\Digest\{DigestReader,FactAmplitude,FactSentence}` + les DTO, une route HTML et une JSON. **Le noyau n'a rien eu à changer, aucune table non plus** — `EventStore::between()` du lot 2 lit `tick-N..tick` au lieu de `0..tick`, c'est le même appel. Trois blocs : un **bilan agrégé** non trié (c'est là que vivent les ~90 matchs ordinaires d'une fenêtre : agrégés ils informent, listés ils noient), 8 **faits marquants** triés `amplitude × poids_du_rôle × fraîcheur` (forme de `14-` §3, une base et deux modificateurs bornés ; la « proximité » est `ClubMentions::roleOf()`, déjà seul à savoir à quel titre un club est nommé), et 4 faits du **monde** racontés sans point de vue. Une phrase par `match` sur classe, **pas** le moteur de détecteurs déclaratifs de `14-` §9 — c'est le `NarrativeSystem` de la Phase 6, et un moteur de règles à un seul consommateur serait l'anticipation refusée partout ailleurs. Page à **41,9 ms**, moins cher que l'histoire complète (63,3 ms). Détail dans `packages/api/README.md`.

⚠️ **Le digest est le contrôle qualité des seuils promis par `14-` §9, et il a rendu son verdict — c'est le vrai livrable du lot.** (1) **La densité des Faits varie d'un facteur ~30 dans l'année** : ~178 au mois du mercato, moins de 3 par mois pendant l'intersaison. La moyenne « ~115 Faits par trimestre » du lot 0 ne décrit **aucune** fenêtre réelle, et comme `dix-ans` s'arrête au jour 0 d'une année, son digest par défaut est vide (12 Faits, zéro pour le club) — pas une panne, l'intersaison. (2) **Sur une fenêtre représentative il se lit vraiment en trente secondes.** (3) **Ce qui manque au journal est enfin visible en creux** : le digest sait dire « large victoire 5-1 » mais jamais *pourquoi* (ni buteur, ni blessure, ni débuts), pendant que `TransferCounterDemanded` pèse 10,6 % des Faits d'une fenêtre — les deux bouts du même réglage, inscrits en **D7**. (4) Les noms sont des identifiants (`Joueur 261`), inscrit en **D8**. **Rien de tout ça n'a été corrigé dans le lot, délibérément** : ajouter des Faits se décide le digest sous les yeux. Deux gardes mécanisés au passage : `DigestCoversEveryFactTest` (tout Fait est noté **ou** exclu avec sa raison, copie de `EveryFactIsPlacedOrExcludedTest`) et l'exhaustivité de `ROLE_WEIGHTS` sur `ClubRole`, tenue par PHPStan qui signale un `?? 1.0` comme mort — un repli silencieux aurait donné le poids **maximal** à un rôle inconnu. `FactAmplitude::of()` rend `?float` et non `float` : `null` = « type non noté » contre `0.0` = « instance ordinaire », distinction sans laquelle le test d'exhaustivité ne peut rien dire — il a d'ailleurs échoué à sa première exécution faute d'elle.

**Phase 4 close le 2026-08-09 : SSE requalifié en Phase 5, et les deux dettes que la phase avait ouvertes sont soldées.** Le lot 4 (SSE) n'est pas coché mais **déplacé**, pour cinq raisons dont trois n'avaient pas été vues : aucun consommateur (`game-web` n'existe pas, l'admin est mono-utilisateur) ; **le transport ne peut pas être choisi honnêtement** (PostgreSQL seul laisse le sondage d'`events`, qui immobilise un worker par client, ou `LISTEN`/`NOTIFY`, que **PDO n'expose pas** — il faut `ext-pgsql`, absent de la CI) ; **`artisan serve` est mono-processus**, donc une connexion SSE tenue bloquerait tout l'admin ; on câblerait la livraison temps réel d'un flux que le lot 3 venait de mesurer à 10,6 % de procédure ; et ce que SSE a de structurant (publier **hors** transaction) est déjà écrit dans `AdvanceWorld` et `13-` §8. Son vrai moment est celui où un humain regarde un match se dérouler — Phase 5, avec l'inbox d'intentions dont il est la moitié symétrique.

**D7, moitié « bavard » : `TransferCounterDemanded` n'était pas un Fait de trop, c'était un Fait mal classé** — et le bon diagnostic a changé la correction. Les trois critères de `16-` §1 le désignaient : adressé à un acheteur précis, attend une réponse, porte une échéance (le docblock de `responseGraceTicks` se décrivait déjà comme « l'échéance sans le canal »). Le supprimer aurait cassé le canal que `SubmittedBuyerIntentSource` attend pour la Phase 5 ; le **reclasser** ne coûte rien, parce que `TransferAgreed` **et** `TransferNegotiationBroken` portaient déjà `round` et que toute négociation se clôt par l'un des deux — distribution des tours identique au chiffre près (85 négociations, médiane 2, moyenne 2,518, 41 au premier tour). Livré : `Core\Messaging\RequestQueue`, `SystemContext::ask()` (jumeau d'`emit()`), `StepResult::$requests`, `Football\Requests\TransferCounterOffered`. **Le canal s'arrête à `StepResult`** — `AdvanceWorld` le lit et l'ignore délibérément, la livraison est Phase 5 ; le gain immédiat est que ces questions ne sont plus **écrites** (96 lignes sur 4 610 pour `dix-ans`, ~2 % du journal). ⚠️ J'ai d'abord annoncé −15 %, en comparant deux `pg_total_relation_size('events')` — qui agrègent **tous** les mondes de la base et bougent avec le vacuum. Le disque n'était de toute façon pas l'argument : ce qui compte est que 10,6 % des Faits d'une fenêtre de mercato n'aient plus à être filtrés à la lecture. ⚠️ La dette accusait les **trois** Faits de négociation : deux franchissent bien un seuil de `16-` §2 (un club qui s'engage, une rupture irréversible) et restent. Garde mécanisée dans les deux sens : `SnapshotConformanceTest` exige désormais qu'aucune classe de `Requests/` n'ait de clé de registre (éprouvé par sabotage).

**D8 : `Football\Support\NameBook`, et la contrainte a dicté la conception.** Tirer un nom dans le flux RNG partagé aurait décalé toutes les allocations suivantes et **changé le monde entier** — le piège des 18 scouts, pour du cosmétique. Le nom est donc une fonction pure de `(worldSeed, entityId)` via `Hash::mixAll()`, **zéro tirage consommé** ; la graine y entre parce que les `EntityId` n'en dépendent pas. ⚠️ Les **clubs** ne peuvent pas être nommés comme les joueurs : 18 noms tirés parmi 320 combinaisons se heurtent une fois sur deux, d'où un parcours de tous les couples (lieu, forme) à pas premier avec leur nombre — unicité **structurelle**, pas probable. Le digest se lit maintenant « Wilfried Lefevre raccroche à 30 ans, sous les couleurs de FC Merignac » là où il disait « Joueur 261 ». D7 garde sa moitié « muet » (ni buteur, ni blessure, ni débuts), requalifiée : elle suppose que `MatchSystem` produise la matière, rejoint **D2**, et appartient au moteur L1 de la Phase 6.

⚠️ **Deux faits de méthode de ce lot.** (1) **La preuve d'un renommage est une empreinte appariée, et elle doit être partielle pour dire quelque chose** : les hashes globaux bougent forcément (l'un perd 141 événements, l'autre gagne des noms). Ce qui prouve le no-op, c'est la **séquence d'événements privée du type retiré** et l'**état privé des noms** — tous deux identiques octet pour octet, plus `EntityId` et compteur d'entités inchangés. (2) **Un monde persisté vieillit par rapport au code.** Régénérer `dix-ans` a donné des comptes différents (`transfer_agreed` 17 → 20, `contract_signed` 819 → 823…), ce qui ressemblait à une régression de ce lot ; c'était le correctif du marché (`b963783`), depuis lequel le monde n'avait jamais été régénéré. Vérifié en rejouant l'arbre d'avant ce commit, qui reproduit les anciens chiffres **exactement**. Les chiffres d'un monde en base ne valent que datés du commit qui les a écrits.

**Correction du marché après coup, le 2026-08-08 : il n'échangeait que deux postes sur quatre.** Mesurée en soldant la dette D5, la ventilation des transferts payants donnait GK 28,4 % / DEF 62,5 % / MID 9,2 % / **ATT 0,0 %** — **zéro attaquant sur 261 transferts**, six graines, 120 saisons. La cause est la composition de trois faits anodins pris séparément : `NpcBuyerIntentSource::neededPosition()` visait le **premier** poste sous-effectif dans l'ordre de l'enum ; `SquadComposition::targets()` somme à 22 pour un `targetSquadSize` de 20 ; et l'effectif réel tourne à 16,5. Un club étant donc en déficit **partout en permanence**, « le premier poste en déficit » n'était pas une priorité mais **une constante** — et comme un club n'ouvre qu'une négociation par an, cette constante était tout son marché. ⚠️ **La leçon dépasse ce cas : un ordre total sur une énumération est légitime comme départage (`12-` §2) et devient un défaut silencieux dès qu'il sert de *critère* sur une population où le prédicat est vrai partout.** Corrigé en classant les cibles sur **tous** les postes en déficit à la fois, par `score = (qualité perçue / prix estimé) × (1 + needWeightSpan × déficit/cible)` — forme de `14-` §3, un seul modificateur, borné par construction ; c'est aussi le motif que `ContractSystem::pick()` tenait déjà. Résultat à graines appariées : ATT 0,0 → 21,4 %, MID 9,2 → 23,6 %, DEF 62,5 → 37,0 %, volume total quasi inchangé (261 → 276), **club-années sans gardien 2,41 → 2,50 % avec une disette maximale toujours d'un an**, et **aucun effet détectable sur l'équilibre compétitif** (Gini et rotation à 4/2 au test du signe, ce qui arrive une fois sur trois à pile ou face — et les deux montent *ensemble*, alors qu'ils vont d'ordinaire en sens inverse). La campagne `--set needWeightSpan=0` dit que **c'est la mise en commun des postes qui a corrigé la distorsion, pas la pondération** : à span nul la distribution est déjà saine, le paramètre ne déplace que le nombre de gardiens achetés (+43 %). Détail complet dans `docs/17-` §Après le chantier ; le déficit permanent lui-même reste ouvert en **D6**. ⚠️ **Moitié du lot hors noyau, et c'est la leçon durable** : la ventilation par poste n'existait **nulle part**, et les club-années sans gardien n'existaient que comme *plafond muet* dans `FieldableSquadTest` — assez pour attraper une aggravation, pas pour empêcher un chiffre mono-graine de circuler quatre jours dans trois documents. `Metrics\Sampler` les compte et `Report\TextReport` les imprime désormais : **une métrique qui compte doit s'imprimer, pas seulement s'asserter.**

**Dette D1 soldée le 2026-08-08 : la CI couvre les cinq paquets qui ont des tests.** `worldgen`, `host` et `api` s'ajoutent à `kernel` et `harness`, les deux derniers sur un vrai service Postgres qui **reproduit `docker-compose.yml` à l'identique** (même image, mêmes identifiants, même port 54329, donc les défauts de `DatabaseConfig` et aucun `FLAIR_DB_*` à passer). Trois silences trouvés, tous de la même famille — *un mécanisme qui ne se déclenche pas ne se plaint jamais*. (1) **Les suites à base de données se skippent proprement quand aucune base n'est joignable**, ce qui est juste en local et **faux en CI** : un service mal câblé rendrait un job vert n'ayant rien exécuté. D'où `--fail-on-skipped` sur `host` et `api`, éprouvé par sabotage (base arrêtée : `exit=0` sans le drapeau, `exit=1` avec) et sans risque de faux positif — ce sont les trois seuls `markTestSkipped` du dépôt. (2) **Le déclencheur `push` pointait sur `main` alors que la branche est `master`** : il n'avait donc jamais tourné une seule fois, seules les PR lançaient la CI. (3) Quatre `phpunit.xml` validaient contre un schéma déprécié (`verbose="true"`, mort depuis PHPUnit 10) — nettoyé. Détail dans `.github/workflows/README.md`, y compris les deux écarts assumés entre CI et poste de dev (PHP 8.3 contre plus récent, et aucun `composer.lock` suivi).

Une observation du lot 0 laissée **non corrigée**, hors périmètre : **une fenêtre de dix ans ment sur l'équilibre compétitif** (le club 17 gagne 7 des 9 saisons en base, quand le harness sur le *même build* et la même graine donne 12 champions distincts en 39 saisons, Gini 0,608, rotation 48,9 %) — aucune régression conclue, la comparer aux chiffres notés dans les documents serait la comparaison explicitement interdite.

Ce qui existe et tourne de bout en bout — **détail classe par classe dans `packages/kernel/README.md`, pas dupliqué ici** :
- Le socle générique (`src/Core/` : ECS, messagerie Fait/DecisionRequest/Intent, `Scheduler`/`OutQueue`/`RequestQueue`, `Pipeline`, PRNG 32 bits, `Ruleset` versionné imbriqué par sous-domaine, `Simulation::step()`, sérialisation `Snapshot/`).
- Le domaine football (`src/Football/`) : vieillissement (retraite + progression des compétences), entraînement, intake de jeunes (ferme la boucle démographique), calendrier + match L0 (Dixon-Coles) + classement, grand livre monétaire + installations + contrats + perception (Phase 2, ci-dessus). Aucun système n'écrit `Employment`/`Scout` : le staff est semé au genesis, comme `Facilities`/`Finances`. `src/Football/Requests/` porte les questions du domaine — jamais enregistrées dans `FootballTypes`, et `SnapshotConformanceTest` le vérifie dans les deux sens.
- Pipeline : déclaré une seule fois dans `Football\FootballPipeline` (`bin/demo.php`, harness et futur `host` le lisent), et **son ordre d'exécution est dérivé** des déclarations `reads()`/`writes()`/`removes()` par `Core\Pipeline\SystemGraph` — tri topologique, ex æquo départagés de façon stable par la liste déclarée, jamais par le nom. Ordre obtenu : `FacilitiesSystem`, `SquadSystem`, `TrainingSystem`, `RetirementSystem`, `FinanceSystem`, `PlayerDevelopmentSystem`, `YouthIntakeSystem`, `CalendarSystem`, `MatchSystem`, `CompetitionSystem`, `ContractSystem` (`YouthIntakeSystem` est descendu de la 1re a la 6e place le 2026-08-04, quand il s'est mis a lire l'effectif en place pour savoir quel poste manque au club — l'ordre s'est re-derive seul, personne n'a arbitre). `SquadSystem` (application) et `ContractSystem` (décision) encadrent tout le reste : le décideur doit lire les compétences et `Finances`, donc passer après leurs writers ; l'applicateur doit écrire `SquadMembership` avant `TrainingSystem`/`MatchSystem`. ⚠️ Ces deux contraintes **sont** satisfaites simultanément et le graphe de dépendances est acyclique — contrairement à ce que ce paragraphe affirmait avant le 2026-08-03. La vraie contrainte, elle bien réelle, est que l'*effet* d'une décision ne peut pas s'appliquer dans le tick qui l'a prise : d'où décider tard, émettre un Fait, appliquer tôt au tick suivant.
- `packages/worldgen/` : la genèse d'un monde — clubs, compétition, staff (`StaffFactory`, semé **après** les joueurs pour ne pas décaler leurs `EntityId`) et population de départ. Ne dépend que de `kernel`.
- `packages/host/` : un monde qui vit en base — event store, snapshots, verrou mono-writer, garde `Ruleset`, CLI. Détail dans `packages/host/README.md`.
- `packages/api/` : le monde visible — couche de lecture unique (`Flair\Api\Read\`), pages d'administration et JSON sur les mêmes DTO, Laravel 13. Détail dans `packages/api/README.md`.
- `packages/harness/` : comparaison à graines appariées (baseline vs `Ruleset` modifié, `RulesetOverride::withFields()`), métriques d'effectif/pyramide des âges/équilibre compétitif (Gini titres + revenus)/graphe d'événements/jugement des recruteurs en regard du classement, test de déterminisme, test de conservation monétaire, test de continuité de snapshot, CI. ⚠️ `RulesetOverride::withFields()` reconstruit `Balance` en entier : **tout groupe omis de cette reconstruction repart à ses défauts** — `PositionBalance` l'était silencieusement jusqu'au 2026-08-05.

Décision à connaître avant de toucher au moteur de match : `exp()` est la première fonction transcendante autorisée dans le noyau (une seule machine exécute le noyau, donc pas de risque de portabilité cross-plateforme — cf. `docs/13-` §4.8 et `packages/kernel/README.md`). `PlayerFactory` reste volontairement sans fonction transcendante (`Beta(1,k)` plutôt qu'une log-normale) pour un besoin différent, la portabilité cross-plateforme de la loi de talent — à revisiter si une comparaison de hash inter-machines apparaît un jour. `PerceptionModel` suit le même arbitrage que `PlayerFactory` (Irwin-Hall plutôt que Box-Muller) : ni `log` ni `cos`, seulement `sqrt`, algébrique et exactement spécifiée par IEEE-754.

`TickContext.intents` **a son premier consommateur réel** depuis le 2026-08-07 (`Football\Intents\SubmittedBuyerIntentSource`). Ce qui manque encore, c'est ce qui le **remplit** en production : l'inbox d'intentions de `host` (`docs/13-` §8), Phase 5.

**Ne pas construire une brique hors du périmètre de la phase en cours** sauf demande explicite — voir `15-roadmap.md` §4 pour les critères de sortie de chaque phase, §5 pour ce qu'il ne faut explicitement pas faire.
